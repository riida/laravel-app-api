openapi: 3.1.0
info:
  version: 1.0.0
  title: laravel sample API
  description: A sample API in the OpenAPI 3.1 specification
servers:
  - url: http://localhost:8081
paths:
  /login:
    $ref: './paths/login.yaml'
  /auth/info:
    $ref: './paths/auth-info.yaml'
  /product:
    $ref: './paths/product.yaml'
  /products:
    $ref: './paths/products.yaml'
tags:
  - name: auth
    description: 認証関連
  - name: product
    description: 商品関連
externalDocs:
  description: Find out more about Swagger
  url: http://swagger.io
